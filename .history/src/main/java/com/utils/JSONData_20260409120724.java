package com.utils;

import java.nio.file.Files;

public class JSONData {

    private static JSONData data;

    static{

        try {
            String content = new String(Files.readAllBytes(null))
        } catch (Exception e) {
            // TODO: handle exception
        }
    }

}
